export const environment = {
  production: true,
  /**
   * Base URL for the API (no trailing slash).
   * Example: 'https://api.horusacademy.com'
   * If your frontend and API are served from the same domain, keep this empty.
   */
  apiBaseUrl: '',
} as const;

